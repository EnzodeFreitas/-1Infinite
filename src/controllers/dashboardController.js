var dashboardModel = require("../models/dashboardModel");

function salvarRota(req, res){
    var geo = req.body.geojson;
    var distancia = req.body.distancia_km
    dashboardModel.
};


modele.exports = {
    salvarRota,

}